1. Rewrite byte handling to use bytearray methods
2. Make Input through text representation
3. Add different encodings
4. Make byteorder affect on text repr in long encodings(more than 1 byte)
5. Search of byte substring in file
6. Make app usable through cmd(no gui)
7. Make readme page
8. Make binary file work(problem with PyQt5 dependencies, it is stated that pyinstaller should work with 5.15.3 and lower, but packager still cannot import PyQt5)
